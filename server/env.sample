# This is a sample .env file for use in local development.
# Duplicate this file as .env in the root of the project server
# and update the environment variables to match your
# desired config
#


# A PostgreSQL connection
DB_NAME=users
DB_USER=postgres
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432

# server port
PORT=5000

# secret variables for jwt and crypt
SECRET_KEY=strong_secret_key48925!==
SALT=8